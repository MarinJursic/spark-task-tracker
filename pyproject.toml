[tool.ruff]
target-version = "py312"
line-length = 100
exclude = ["backend/tasks/migrations"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "DJ"]

[tool.ruff.format]
quote-style = "double"
